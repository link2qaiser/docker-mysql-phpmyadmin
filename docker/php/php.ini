; Increase the maximum allowed size for POST data
post_max_size = 100M

; Increase the maximum allowed size for uploaded files
upload_max_filesize = 100M

; Increase the memory limit
memory_limit = 256M

; Ensure session handling is properly configured
session.save_path = "/tmp"
